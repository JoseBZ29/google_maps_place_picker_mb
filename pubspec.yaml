name: google_maps_place_picker_mb
description: A Flutter plugin which provides 'Picking Place' using Google Maps
  widget. (MB version)
version: 3.1.2
homepage: https://github.com/martin-braun-net/google_maps_place_picker_mb

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^10.0.1
  google_api_headers: ^4.1.1
  google_maps_flutter: ^2.6.1
  flutter_google_maps_webservices: ^1.1.1
  http: ^1.1.0
  provider: ^6.0.5
  tuple: ^2.0.2
  uuid: ^4.0.0
  package_info_plus: ^8.0.0
  googlemaps_flutter_webservices: ^1.0.2
  google_places_flutter: ^2.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter

dependency_overrides:
  google_places_flutter:
    git:
      url: https://github.com/JoseBZ29/Google-AutoComplete-TextField-Flutter.git
      ref: master

flutter: null

false_secrets:
  - /example/lib/keys.dart
  - /example/android/app/src/main/AndroidManifest.xml
  - /example/ios/Runner/AppDelegate.swift
